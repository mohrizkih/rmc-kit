import 'package:flutter/material.dart';

class SensorHistory extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(child: Text('HaloHalo Bandung')),
    );
  }
}